# InsightDash (Streamlit + Pandas + Plotly)

A clean, interactive analytics dashboard demonstrating data cleaning, KPIs,
interactive filtering, charts, and data quality checks.

Run locally:
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run app/Home.py
